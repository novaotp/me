import { Metadata } from "next";

export const metadata: Metadata = {
    title: "My Works"
}

const Page = () => {
    return (
        <div>
            <h1>This is the projects page.</h1>
        </div>
    )
}

export default Page;
