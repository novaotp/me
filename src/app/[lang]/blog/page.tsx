import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Blog"
}

const Page = () => {
    return (
        <div>
            <h1>This is the blog page.</h1>
        </div>
    )
}

export default Page;
