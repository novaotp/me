import { Metadata } from "next";

export const metadata: Metadata = {
    title: "About Me"
}

const Page = () => {
    return (
        <div>
            <h1>This is the about page.</h1>
        </div>
    )
}

export default Page;
